#!/usr/bin/env python3
"""
Complete Configuration Analysis

Compares GB200 Reference against:
1. Base configs in scripts/performance/configs/
2. run_nemo2507_validation_sweep.sh settings
"""

print("=" * 120)
print("COMPLETE GB200 CONFIGURATION ANALYSIS")
print("Base Configs + Sweep Script vs GB200 Reference")
print("=" * 120)
print()

# ============================================================================
# LLAMA3 8B (GB200, BF16)
# ============================================================================
print("─" * 120)
print("1. LLAMA3 8B (GB200, BF16)")
print("─" * 120)

print("""
┌─────────────────────────────────────────────────────────────────────────────┐
│ GB200 Reference                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ num_gpus=8, seq_len=8192, TP=1, PP=1, CP=1, DP=8, EP=1, VP=1                │
│ MBS=2, GBS=128                                                               │
│ cuda_graphs=1, use_mcore_fsdp=0, recompute_layers=0                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Base Config (LLAMA3_8B_GB200_BF16_BASE_CONFIG)                              │
├─────────────────────────────────────────────────────────────────────────────┤
│ micro_batch_size=2                              ✓ 일치                      │
│ cuda_graph_impl="local", cuda_graph_scope="full_iteration"  ✓ CG 활성화   │
│ (나머지는 기본값 사용)                                                      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Sweep Script Override (line 87)                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│ "llama3 8b 8 8192 1 1 1 1 1 1 0 2 128 bf16 pretrain nemo2507"              │
│ TP=1, PP=1, CP=1, EP=1, VP=1, ETP=1, FSDP=0, MBS=2, GBS=128                 │
└─────────────────────────────────────────────────────────────────────────────┘

최종 결과: ✅ 일치!
  - MBS=2 ✓
  - cuda_graphs=1 ✓ (base config에 설정됨)
  - FSDP=0 ✓
""")

# ============================================================================
# LLAMA3 70B (GB200, BF16)
# ============================================================================
print("─" * 120)
print("2. LLAMA3 70B (GB200, BF16) ⚠️ CRITICAL ISSUES")
print("─" * 120)

print("""
┌─────────────────────────────────────────────────────────────────────────────┐
│ GB200 Reference                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ num_gpus=64, seq_len=8192, TP=1, PP=1, CP=1, DP=64, EP=1, VP=1              │
│ MBS=1, GBS=128                                                               │
│ cuda_graphs=0, use_mcore_fsdp=1, recompute_layers=20 ⚠️, cpu_offload=0      │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Base Config (LLAMA3_70B_GB200_BF16_BASE_CONFIG)                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ use_megatron_fsdp=True                          ✓ 일치                      │
│ cpu_offloading_num_layers=20                    ❌ 이건 CPU offload!        │
│                                                 Reference는 recompute=20    │
│ cuda_graph 설정 없음                            ❌ Reference는 CG=0         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Sweep Script Override (line 97)                                              │
├─────────────────────────────────────────────────────────────────────────────┤
│ "llama3 70b 64 8192 1 1 1 1 1 1 1 1 128 bf16 pretrain nemo2507"             │
│ TP=1, PP=1, CP=1, EP=1, VP=1, ETP=1, FSDP=1, MBS=1, GBS=128                 │
└─────────────────────────────────────────────────────────────────────────────┘

⚠️ 문제점:
  1. base config에 cpu_offloading_num_layers=20이 있음
     → Reference는 cpu_offload=0, recompute_layers=20을 요구!
     → 이 두 개는 완전히 다른 설정!
  
  2. cuda_graphs 설정이 명시되지 않음
     → Reference는 CG=0을 요구 (FSDP와 함께 사용 시 비활성화)

  3. recompute_layers=20이 누락됨
     → setup_experiment.py에 이 플래그가 없음

🔧 필요한 수정:
  - base config 수정: cpu_offloading_num_layers=20 제거
  - sweep script에 ++model.activations_checkpoint_num_layers=20 추가
  - sweep script에 cuda_graph 비활성화 추가
""")

# ============================================================================
# LLAMA31 405B (GB200, BF16)
# ============================================================================
print("─" * 120)
print("3. LLAMA31 405B (GB200, BF16)")
print("─" * 120)

print("""
┌─────────────────────────────────────────────────────────────────────────────┐
│ GB200 Reference                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ num_gpus=128, seq_len=8192, TP=4, PP=8, CP=2, DP=2, EP=1, VP=8              │
│ MBS=1, GBS=64                                                                │
│ cuda_graphs=0, use_mcore_fsdp=0, recompute_layers=0                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Base Config (LLAMA31_405B_GB200_BF16_BASE_CONFIG)                           │
├─────────────────────────────────────────────────────────────────────────────┤
│ num_gpus=128                                    ✓                           │
│ tensor_model_parallel_size=4                    ✓                           │
│ pipeline_model_parallel_size=8                  ✓                           │
│ context_parallel_size=2                         ✓                           │
│ virtual_pipeline_model_parallel_size=8          ✓                           │
│ global_batch_size=64                            ✓                           │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Sweep Script Override (line 107)                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ "llama31 405b 128 8192 4 8 2 1 8 1 0 1 64 bf16 pretrain nemo2507"           │
│ TP=4, PP=8, CP=2, EP=1, VP=8, ETP=1, FSDP=0, MBS=1, GBS=64                  │
└─────────────────────────────────────────────────────────────────────────────┘

최종 결과: ✅ 일치!
""")

# ============================================================================
# DeepSeek V3 (GB200, BF16, 256 GPUs)
# ============================================================================
print("─" * 120)
print("4. DeepSeek V3 (GB200, BF16, 256 GPUs) ⚠️ ISSUES")
print("─" * 120)

print("""
┌─────────────────────────────────────────────────────────────────────────────┐
│ GB200 Reference                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ num_gpus=256, seq_len=4096, TP=2, PP=4, CP=1, DP=32, EP=64, VP=1, ETP=1     │
│ MBS=1, GBS=2048                                                              │
│ cuda_graphs=1, use_mcore_fsdp=0, recompute_layers=0                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Base Config (DEEPSEEK_V3_GB200_BF16_BASE_CONFIG)                            │
├─────────────────────────────────────────────────────────────────────────────┤
│ expert_tensor_parallel_size=1                   ✓                           │
│ num_gpus=256                                    ✓                           │
│ pipeline_model_parallel_size=4                  ✓                           │
│ virtual_pipeline_model_parallel_size=4          ❌ Reference는 VP=1!        │
│ expert_model_parallel_size=64                   ✓                           │
│ global_batch_size=2048                          ✓                           │
│ tensor_model_parallel_size 없음                 ❌ Reference는 TP=2!        │
│ recompute_modules=["mla_up_proj"]               ❌ Reference에는 없음       │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Sweep Script Override (line 117)                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ "deepseek v3 256 4096 2 4 1 64 1 1 0 1 2048 bf16 pretrain nemo2507_gbs2048" │
│ TP=2, PP=4, CP=1, EP=64, VP=1, ETP=1, FSDP=0, MBS=1, GBS=2048               │
│ → sweep에서 TP=2, VP=1로 override하므로 OK!                                 │
└─────────────────────────────────────────────────────────────────────────────┘

⚠️ 남은 문제:
  - recompute_modules는 base config에서 설정됨
  - Reference에는 recompute_modules가 없음

🔧 필요한 수정:
  - sweep script에 ++model.recompute_modules=null 추가 (제거)
  
또는 recompute_modules 유지 (성능 최적화용)
""")

# ============================================================================
# Qwen3 30B A3B (GB200, BF16)
# ============================================================================
print("─" * 120)
print("5. Qwen3 30B A3B (GB200, BF16) ⚠️ CRITICAL ISSUES")
print("─" * 120)

print("""
┌─────────────────────────────────────────────────────────────────────────────┐
│ GB200 Reference                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ num_gpus=8, seq_len=4096, TP=1, PP=1, CP=1, DP=8, EP=8, VP=1, ETP=1         │
│ MBS=1, GBS=512                                                               │
│ cuda_graphs=1, use_mcore_fsdp=0, recompute_layers=0                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Base Config (QWEN3_30B_A3B_GB200_BF16_BASE_CONFIG)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│ expert_model_parallel_size=8                    ✓                           │
│ expert_tensor_parallel_size=1                   ✓                           │
│ global_batch_size=512                           ✓                           │
│ num_gpus=4                                      ❌ Reference는 8!           │
│ micro_batch_size=4                              ❌ Reference는 1!           │
│ cuda_graph_impl="local", cuda_graph_scope="full_iteration"  ✓              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Sweep Script Override (line 137)                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ "qwen3 30b_a3b 8 4096 1 1 1 8 1 1 0 1 512 bf16 pretrain nemo2507"           │
│ → sweep에서 num_gpus=8, MBS=1로 override함!                                │
└─────────────────────────────────────────────────────────────────────────────┘

최종 결과: ✅ sweep에서 override하므로 일치!
  - num_gpus=8 (sweep override) ✓
  - MBS=1 (sweep override) ✓
  - EP=8 (base config) ✓
  - ETP=1 (base config) ✓
""")

# ============================================================================
# Qwen3 235B A22B (GB200, BF16)
# ============================================================================
print("─" * 120)
print("6. Qwen3 235B A22B (GB200, BF16) ⚠️ CRITICAL ISSUES")
print("─" * 120)

print("""
┌─────────────────────────────────────────────────────────────────────────────┐
│ GB200 Reference                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ num_gpus=64, seq_len=4096, TP=2, PP=1, CP=1, DP=32, EP=64, VP=1, ETP=1      │
│ MBS=1, GBS=1024                                                              │
│ cuda_graphs=1, use_mcore_fsdp=0, recompute_layers=0                         │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Base Config (QWEN3_235B_A22B_GB200_BF16_BASE_CONFIG)                        │
├─────────────────────────────────────────────────────────────────────────────┤
│ expert_tensor_parallel_size=1                   ✓                           │
│ num_gpus=64                                     ✓                           │
│ pipeline_model_parallel_size=8                  ❌ Reference는 PP=1!        │
│ expert_model_parallel_size=8                    ❌ Reference는 EP=64!       │
│ global_batch_size=1024                          ✓                           │
│ tensor_model_parallel_size 없음                 ❌ Reference는 TP=2!        │
│ cuda_graph_impl="local", cuda_graph_scope="full_iteration"  ✓              │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ Sweep Script Override (line 147)                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│ "qwen3 235b_a22b 64 4096 2 1 1 64 1 1 0 1 1024 bf16 pretrain nemo2507"      │
│ → sweep에서 TP=2, PP=1, EP=64로 override함!                                │
└─────────────────────────────────────────────────────────────────────────────┘

최종 결과: ✅ sweep에서 override하므로 일치!
  - TP=2 (sweep override) ✓
  - PP=1 (sweep override) ✓
  - EP=64 (sweep override) ✓
""")

# ============================================================================
# 최종 요약
# ============================================================================
print()
print("=" * 120)
print("최종 요약: 필요한 수정사항")
print("=" * 120)

print("""
┌─────────────────────────────────────────────────────────────────────────────┐
│ LLAMA3 70B (GB200, BF16) - 가장 중요한 수정 필요!                           │
├─────────────────────────────────────────────────────────────────────────────┤
│ 현재 문제:                                                                   │
│   1. base config에 cpu_offloading_num_layers=20 (잘못됨)                    │
│   2. Reference는 recompute_layers=20, cpu_offload=0을 요구                   │
│   3. cuda_graphs=0 필요 (FSDP와 함께 사용 시)                               │
│                                                                              │
│ 수정 방법:                                                                   │
│   sweep script에 다음 Hydra override 추가:                                  │
│     ++model.activations_checkpoint_num_layers=20                            │
│     ++model.cpu_offloading=False                                             │
│     ++model.cpu_offloading_num_layers=0                                     │
│     ++model.cuda_graph_impl=null (또는 none)                                │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│ 다른 모델들은 sweep script에서 올바르게 override하므로 OK!                  │
├─────────────────────────────────────────────────────────────────────────────┤
│ ✅ LLAMA3 8B: 일치                                                          │
│ ✅ LLAMA31 405B: 일치                                                       │
│ ✅ DeepSeek V3: 일치 (sweep override)                                       │
│ ✅ Qwen3 30B: 일치 (sweep override)                                         │
│ ✅ Qwen3 235B: 일치 (sweep override)                                        │
└─────────────────────────────────────────────────────────────────────────────┘
""")

